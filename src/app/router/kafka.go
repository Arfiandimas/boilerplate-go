package router

import (
	"context"
)

func (rtr *router) KafkaProcessor(ctx context.Context) {
	// repo := repositories.NewExample(rtr.dbAdapter)
	// consumerExample2 := kafkaCase.NewSubscribe2(repo)
	// consumerExample := kafkaCase.NewSubscribe(repo)
	// rtr.kafkaProcessorHandle(ctx, []string{"testing"}, "testing", consumerExample2)
	// rtr.kafkaProcessorHandle(ctx, []string{"testing1"}, "testing", consumerExample)
}
